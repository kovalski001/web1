[build]
  builder = "DOCKERFILE"

[deploy]
  startCommand = "/bin/sh -c 'php-fpm -D && php /var/www/backend/init_db.php && nginx -g \"daemon off;\"'"
  healthcheckPath = "/"
  healthcheckTimeout = 30
  restartPolicyType = "ON_FAILURE"